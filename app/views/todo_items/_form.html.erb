<%= form_with(model: [todo_item.todo_list, todo_item]) do |form| %>
  <% if todo_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
        <% todo_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "New Task", style: "display: block" %>
    <%= form.text_field :description %>

    <%= form.submit "Save" %>
  </div>
<% end %>
